# Bounded Agent Authority Lab

Trace read, calculate, recommend, stage, approve, execute, stop and escalate decisions under explicit authority budgets.

## Run

Open `about.html`, then `index.html`. No API key is required.

## Teaching sequence

Context -> Prediction -> Normal path -> Failure injection -> Executive decision -> Export.
