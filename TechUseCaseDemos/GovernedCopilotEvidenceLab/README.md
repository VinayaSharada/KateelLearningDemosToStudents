# Governed Copilot Evidence Lab

Test current, stale, conflicting, missing and injected evidence before a human releases an AI-assisted claim.

## Run

Open `about.html`, then `index.html`. No API key is required.

## Teaching sequence

Context -> Prediction -> Normal path -> Failure injection -> Executive decision -> Export.
