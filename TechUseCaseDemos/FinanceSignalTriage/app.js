document.addEventListener("DOMContentLoaded",()=>{document.getElementById("signalItems").innerHTML=[["Supplier signal","Late deliveries plus rising DPO pressure"],["Customer signal","Complaint spike but stable collections"],["Analyst signal","Negative comment with no forecast or cash corroboration"]].map(([a,b])=>`<div class="generic-item"><span>${a}</span><strong>${b}</strong></div>`).join("");});
